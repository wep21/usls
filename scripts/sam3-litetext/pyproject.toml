[project]
name = "sam3-litetext-tools"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
    "torch",
    "transformers>=5.12",
    "onnx",
    "onnxruntime",
    "nvidia-modelopt[onnx]>=0.44",
    "numpy>=2.0",
]
